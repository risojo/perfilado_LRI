"""Módulos auxiliares del capítulo de Pronóstico.

Cada módulo expone una función ``render()`` que dibuja su contenido dentro
del cuerpo principal de la app ``LRI_inventarios2.0.py``.

Esta carpeta intencionalmente NO se llama ``pages/`` para evitar que
Streamlit la convierta en páginas auto-enrutadas en la barra lateral.
"""
